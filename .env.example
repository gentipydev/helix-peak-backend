# Required. NCBI rejects requests without a contact address, and the app will
# refuse to start if this is unset.
NCBI_EMAIL=you@example.com

# Optional. Seconds to wait on NCBI before giving up with a 502.
# NCBI_TIMEOUT_SECONDS=20
