# Runtime dependencies. Kept free of test tooling so the Docker image, which
# installs from this file, stays lean. Test deps live in requirements-dev.txt.
fastapi==0.128.8
uvicorn[standard]==0.39.0
biopython==1.85
pydantic-settings==2.11.0
python-dotenv==1.2.1
